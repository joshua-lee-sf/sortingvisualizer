import './navbar.scss';
import NavBarItem from './navbaritem';

const NavBar = () => {
    return(
        <nav className="nav-bar-container">
            <NavBarItem/>
        </nav>
    )
}

export default NavBar